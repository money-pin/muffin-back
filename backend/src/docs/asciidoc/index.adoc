= Muffin API 문서
:doctype: book
:icons: font
:source-highlighter: highlightjs
:toc: left
:toclevels: 2
:sectlinks:

[[investment-settlement-result]]
== 정산 결과 팝업 조회 (INVEST-06)

가장 최근 정산 대상 투자의 결과를 조회한다. 정산 완료(SETTLED)면 손익 결과를 반환하고, 그 외에는 사유(`reason`)만 반환한다.

* `SETTLEMENT_PENDING`: 정산 배치가 아직 완료되지 않음(정산 중)
* `NO_INVESTMENT`: 최근 정산 대상 투자가 없음(미투자/취소 포함)

NOTE: 인증은 추후 구현 예정이며, 현재는 임시로 `X-User-Id` 헤더로 사용자를 식별한다. 인증 구현 후에는 토큰에서 사용자를 해석하므로 이 헤더는 제거된다. 인증 실패 시 `401 AUTH_401_001`을 반환한다.
FE 담당자는 invest_date를 로컬에 저장하여 같은 투자 결과 팝업을 1회만 보여줄 수 있도록 한다.

=== 요청

정산 완료 결과가 있는 경우의 요청 예시.

include::{snippets}/settlement-result-settled/http-request.adoc[]

=== 응답 - 정산 완료(결과 있음)

include::{snippets}/settlement-result-settled/http-response.adoc[]
include::{snippets}/settlement-result-settled/response-fields.adoc[]

=== 응답 - 결과 없음 / 정산 중

include::{snippets}/settlement-result-reason/http-response.adoc[]
include::{snippets}/settlement-result-reason/response-fields.adoc[]

[[auth-email-verification]]
== 이메일 인증번호 발송/확인 (Auth-01)

인증된 사용자(`Authorization: Bearer {accessToken}`)가 본인 계정의 이메일로 인증번호를 발송하고 확인한다. <<auth-signup,회원가입>> 이후 아무 때나 호출할 수 있으며, 이메일은 요청 바디가 아니라 access token으로 식별된 계정의 `Auth.email`을 사용한다.

NOTE: 확인(`/verification/confirm`) 성공 시 같은 트랜잭션에서 `Auth.emailVerified`가 `true`로 갱신된다. 정리 배치는 만료된 미인증 기록만 삭제하고 인증 성공 기록은 건드리지 않는다.

=== 발송 - 성공

include::{snippets}/email-verification-send-success/http-request.adoc[]
include::{snippets}/email-verification-send-success/http-response.adoc[]
include::{snippets}/email-verification-send-success/response-fields.adoc[]

=== 발송 - 실패(이미 인증이 완료된 이메일)

include::{snippets}/email-verification-send-already-verified/http-response.adoc[]
include::{snippets}/email-verification-send-already-verified/response-fields.adoc[]

=== 확인 - 성공

include::{snippets}/email-verification-confirm-success/http-request.adoc[]
include::{snippets}/email-verification-confirm-success/http-response.adoc[]
include::{snippets}/email-verification-confirm-success/response-fields.adoc[]

=== 확인 - 실패(코드 불일치)

include::{snippets}/email-verification-confirm-mismatch/http-response.adoc[]
include::{snippets}/email-verification-confirm-mismatch/response-fields.adoc[]

=== 에러 코드

[cols="1,2,3,3"]
|===
|HTTP|code|message|설명

|401|AUTH_401_001|인증이 필요합니다.|Authorization 헤더에 유효한 access token 없음
|404|COMMON_404_001|요청한 리소스를 찾을 수 없습니다.|access token의 userId에 해당하는 Auth 없음(정상 흐름에서는 발생하지 않음)
|409|AUTH_409_002|이미 인증이 완료된 이메일입니다.|이미 emailVerified=true인 계정
|429|AUTH_429_001|잠시 후 다시 시도해주세요.|재발송 쿨다운(기본 30초) 중
|429|AUTH_429_002|오늘 인증번호 전송 횟수를 초과했습니다.|일일 발송 한도(기본 10회) 초과
|400|AUTH_400_001|인증번호가 일치하지 않습니다.|코드 불일치, 또는 발송 이력 자체가 없음
|410|AUTH_410_001|인증번호가 만료되었습니다.|만료(기본 5분)
|423|AUTH_423_001|인증 시도 횟수를 초과하여 잠겼습니다. 인증번호를 다시 요청해 주세요.|연속 시도 한도(기본 5회) 초과
|500|AUTH_500_001|인증 메일 발송에 실패했습니다.|SMTP 발송 실패(운영 환경)
|===

[[auth-signup]]
== 로컬 회원가입 (Auth-01)

이메일/비밀번호/이름과 약관 동의로 로컬 계정을 생성한다. 가입 즉시 로그인 상태가 되어 access token을 응답 바디로, refresh token을 HttpOnly Cookie(`refreshToken`, Path=`/api/auth`, Secure, SameSite=Strict)로 발급한다.

NOTE: 이메일 인증 여부와 무관하게 가입 자체는 즉시 완료된다(emailVerified=false로 시작). 이메일 인증은 가입 후 access token을 실어 별도로 호출한다(<<auth-email-verification>>).

=== 요청 - 성공

include::{snippets}/signup-local-success/http-request.adoc[]
include::{snippets}/signup-local-success/request-fields.adoc[]

=== 응답 - 성공

include::{snippets}/signup-local-success/http-response.adoc[]
include::{snippets}/signup-local-success/response-fields.adoc[]

=== 응답 - 실패(약관 미동의)

include::{snippets}/signup-local-terms-not-agreed/http-response.adoc[]
include::{snippets}/signup-local-terms-not-agreed/response-fields.adoc[]

=== 응답 - 실패(이미 사용 중인 이메일)

include::{snippets}/signup-local-duplicate-email/http-response.adoc[]
include::{snippets}/signup-local-duplicate-email/response-fields.adoc[]

=== 에러 코드

[cols="1,2,3,3"]
|===
|HTTP|code|message|설명

|400|COMMON_400_002|요청 값 검증에 실패했습니다.|이메일 형식 오류 등(errorDetail 참고)
|400|COMMON_400_001|잘못된 요청입니다.|비밀번호 형식 오류(영문+숫자 8~16자, errorDetail 참고)
|400|AUTH_400_002|약관에 동의해야 회원가입할 수 있습니다.|termsAgreed=false
|409|AUTH_409_001|이미 사용 중인 이메일입니다.|이메일 중복
|===

[[auth-login]]
== 로컬 로그인 (Auth-02)

이메일/비밀번호로 로그인하고 access token을 응답 바디로, refresh token을 HttpOnly Cookie(`refreshToken`, Path=`/api/auth`, Secure, SameSite=Strict)로 발급한다.

NOTE: 가입되지 않은 이메일과 비밀번호 불일치는 계정 존재 여부가 노출되지 않도록 동일한 에러(`AUTH_401_002`)로 응답한다.

NOTE: 비밀번호를 5회 연속 틀리면 계정이 30분간 잠긴다(`AUTH_423_002`). 잠긴 뒤에는 올바른 비밀번호를 넣어도 비교 자체를 하지 않고 즉시 거부하며, 30분이 지나면 별도 배치 없이 자동으로 해제되고 실패 횟수도 초기화된다. 잠기기 전에 로그인에 성공하면 그 시점에 실패 횟수가 즉시 초기화된다.

=== 요청 - 성공

include::{snippets}/login-local-success/http-request.adoc[]
include::{snippets}/login-local-success/request-fields.adoc[]

=== 응답 - 성공

include::{snippets}/login-local-success/http-response.adoc[]
include::{snippets}/login-local-success/response-fields.adoc[]

=== 응답 - 실패(이메일/비밀번호 불일치)

include::{snippets}/login-local-invalid-credentials/http-response.adoc[]
include::{snippets}/login-local-invalid-credentials/response-fields.adoc[]

=== 응답 - 실패(탈퇴한 계정)

include::{snippets}/login-local-withdrawn-account/http-response.adoc[]
include::{snippets}/login-local-withdrawn-account/response-fields.adoc[]

=== 응답 - 실패(로그인 시도 잠금)

include::{snippets}/login-local-locked/http-response.adoc[]
include::{snippets}/login-local-locked/response-fields.adoc[]

=== 에러 코드

[cols="1,2,3,3"]
|===
|HTTP|code|message|설명

|400|COMMON_400_002|요청 값 검증에 실패했습니다.|이메일 형식 오류 등(errorDetail 참고)
|401|AUTH_401_002|이메일 또는 비밀번호가 일치하지 않습니다.|가입되지 않은 이메일 또는 비밀번호 불일치
|403|AUTH_403_002|탈퇴한 계정입니다.|status=WITHDRAWN
|403|AUTH_403_003|정지된 계정입니다.|status=SUSPENDED
|423|AUTH_423_002|로그인 시도 횟수를 초과하여 계정이 잠겼습니다. 30분 후 다시 시도해 주세요.|비밀번호 5회 연속 실패(30분 후 자동 해제)
|===

[[auth-token-refresh]]
== Access Token 재발급 (Auth-02)

요청 Cookie의 refresh token(`refreshToken`)으로 access token을 새로 발급한다. 요청 바디는 없고 `Authorization` 헤더도 필요 없다(만료된 access token을 대체하기 위한 API이므로).

NOTE: 재발급할 때마다 refresh token도 함께 회전(rotate)된다. 새 refresh token이 다시 `Set-Cookie`로 내려가고, 재발급에 쓰인 기존 refresh token은 그 즉시 무효화되어 재사용할 수 없다(탈취 재사용 방지).

=== 응답 - 성공

include::{snippets}/token-refresh-success/http-response.adoc[]
include::{snippets}/token-refresh-success/response-fields.adoc[]

=== 응답 - 실패(refresh token 쿠키 없음/유효하지 않음)

include::{snippets}/token-refresh-invalid/http-response.adoc[]
include::{snippets}/token-refresh-invalid/response-fields.adoc[]

=== 에러 코드

[cols="1,2,3,3"]
|===
|HTTP|code|message|설명

|401|AUTH_401_003|유효하지 않은 refresh token입니다.|쿠키 없음, 존재하지 않는 토큰, 만료된 토큰을 모두 동일하게 응답
|403|AUTH_403_002|탈퇴한 계정입니다.|status=WITHDRAWN
|403|AUTH_403_003|정지된 계정입니다.|status=SUSPENDED
|===

[[stats-summary]]
== 수익 통계 조회 (STATS-02)

가입 이후 누적 손익, 최근 7일 누적 수익률 그래프, 수익 TOP3 섹터, 투자 성향을 한 번에 조회한다. 정산 완료(SETTLED)된 투자만 집계한다.

* 누적 수익률: 초기자본 100만원 기준(`누적 손익금 / 1,000,000 × 100`), 소수 첫째자리.
* 그래프: 가장 최근 정산일을 포함한 직전 7일. 각 날짜는 그 날까지의 누적 수익률이며, 투자하지 않은 날은 직전 누적을 그대로 유지한다(손실 시 음수).
* TOP3 섹터: 섹터 누적 수익률(그 섹터 누적 매수금 기준) 내림차순 3개.
* 투자 성향: 섹터 그룹별 누적 매수 비중으로 판정(STABLE/BALANCED/GROWTH/AGGRESSIVE).

NOTE: 인증은 추후 구현 예정이며, 현재는 임시로 `X-User-Id` 헤더로 사용자를 식별한다. 인증 구현 후에는 토큰에서 사용자를 해석하므로 이 헤더는 제거된다. 인증 실패 시 `401 AUTH_401_001`을 반환한다.

=== 요청

include::{snippets}/stats-summary/http-request.adoc[]

=== 응답 - 정상

include::{snippets}/stats-summary/http-response.adoc[]
include::{snippets}/stats-summary/response-fields.adoc[]

=== 응답 - 정산 이력 없음(빈 상태)

정산 완료 이력이 없는 신규 사용자는 `investDate`/`investmentType`이 생략되고 `graph`/`topSectors`가 빈 배열이다.

include::{snippets}/stats-summary-empty/http-response.adoc[]
include::{snippets}/stats-summary-empty/response-fields.adoc[]
